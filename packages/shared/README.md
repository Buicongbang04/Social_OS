# @repo/shared — Shared

> Phase 0

Utility function thuần TypeScript, không phụ thuộc framework (Next.js/NestJS) hay domain logic — dùng chung cho mọi tầng. Xem `docs/06_MONOREPO_STRUCTURE.md#packages` (mục `shared`).

Không đặt Result/Error/Entity type ở đây — các type đó thuộc `packages/core` (Giai đoạn 2).
