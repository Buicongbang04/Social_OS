# @repo/sdk — Shared

> Reserved (chưa xếp lịch cụ thể trong docs/ROADMAP.md)

External SDK phát hành cho bên thứ ba (TypeScript SDK, JavaScript SDK) dùng Runtime API. Xem `docs/api/`, `docs/06_MONOREPO_STRUCTURE.md#packages` (mục `sdk`).

Chưa có code ở Phase 0.
