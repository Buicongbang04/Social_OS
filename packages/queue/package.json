{
  "name": "@repo/queue",
  "private": true,
  "version": "0.0.0",
  "description": "Redis-backed TaskQueue and SchedulerLock implementing the @repo/runtime ports — see docs/runtime/09_RUNTIME_QUEUE.md",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:int": "vitest run --config vitest.int.config.ts",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@repo/core": "workspace:*",
    "@repo/runtime": "workspace:*",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "^22.10.0",
    "eslint": "^9.15.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
