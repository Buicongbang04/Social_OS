# @repo/ui — Shared / Frontend

> Phase 0: mới có `Button` + `cn()` tối thiểu. Bộ component đầy đủ (shadcn/ui, Radix UI) sẽ bổ sung dần theo `docs/frontend/02_DESIGN_SYSTEM.md` khi apps/web có tính năng thật.

```tsx
import { Button } from "@repo/ui";

<Button variant="primary">Save</Button>;
```
