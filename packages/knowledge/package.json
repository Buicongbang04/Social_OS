{
  "name": "@repo/knowledge",
  "private": true,
  "version": "0.0.0",
  "description": "Chunking, embedding and vector search over a workspace's documents. See docs/data/08_VECTOR_DATABASE.md",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.build.json",
    "test:int": "vitest run --config vitest.int.config.ts"
  },
  "dependencies": {
    "@qdrant/js-client-rest": "^1.18.0",
    "@repo/ai": "workspace:*",
    "@repo/core": "workspace:*",
    "@repo/runtime": "workspace:*"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "^22.10.0",
    "eslint": "^9.15.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
