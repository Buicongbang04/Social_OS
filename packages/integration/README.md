# @repo/integration — Shared

> Reserved for Phase 3 — Social Platform (Integration Layer)

Connector SDK, Social API client, Webhook handling cho nền tảng bên ngoài (Facebook, Instagram, TikTok, Telegram, Lark...). Đổi tên từ `packages/social` trong tài liệu gốc để khớp với **Integration Domain** — xem `docs/03_DOMAIN_MODEL.md`.

Không liên quan tới `docs/social_network/` (mạng xã hội nội bộ, future-vision).

Chưa có code ở Phase 0.
