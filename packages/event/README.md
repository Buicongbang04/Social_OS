# @repo/event — Shared

> Reserved for Phase 1 — AI Runtime

Event SDK dùng chung cho Event Bus (publish/subscribe, versioned event schema, correlation ID). Xem `docs/kernel/11_EVENT_BUS.md`, `docs/runtime/13_EVENT_BUS.md`.

Chưa có code ở Phase 0.
