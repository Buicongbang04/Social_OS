{
  "name": "@repo/auth",
  "private": true,
  "version": "0.0.0",
  "description": "Framework-agnostic auth primitives: argon2id password hashing, opaque refresh tokens, permission evaluation — see docs/platform/06_AUTHENTICATION.md",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@repo/core": "workspace:*",
    "@repo/domain": "workspace:*",
    "argon2": "^0.41.1"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "^22.10.0",
    "eslint": "^9.15.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
