# services/worker — Backend

> Reserved for Phase 1 — AI Runtime

Worker Cluster: LLM Worker, Browser Worker, Python Worker, Media Worker, Connector Worker. Xem `docs/runtime/02_WORKER_DISPATCHER.md`, `docs/runtime/03_WORKER_POOL.md`.

Chưa có code ở Phase 0.
